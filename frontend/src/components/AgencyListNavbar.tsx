const AgencyListNavbar = () => {
    return (
        <header style={{ padding: '20px', background: '#FFF', textAlign: 'left', border: '1px solid #000', borderRadius: '5px', backgroundColor: '#f0f0f0' }}>
            <h1>ask.gov</h1>
        </header>

    );
};

export default AgencyListNavbar;
