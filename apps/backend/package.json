{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "main": "manage.py",
  "scripts": {
    "setup": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
    "dev": "source .venv/bin/activate && python3 manage.py runserver",
    "build": "",
    "start": "python3 manage.py runserver",
    "lint": ""
  }
}
