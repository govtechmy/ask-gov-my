apiVersion: apps/v1
kind: Deployment
metadata:
  name: askgov-backend
  labels:
    app: askgov-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: askgov-backend
  template:
    metadata:
      labels:
        app: askgov-backend
    spec:
      containers:
        - name: askgov-backend
          image: DEPLOYMENT_IMAGE
          ports:
            - containerPort: 8000
